package dow_v2.Partida;

/*import java.util.LinkedList;
import java.util.List;
import java.util.Scanner;*/

public class Partida {
	public static void main(String [] args) {
		/*Jugadores: 2 a 5
		System.out.println("Indique el número de jugadores: ");
		Scanner sc = new Scanner(System.in);
		do {
		int numJugadores = sc.nextInt();
		} while (numJugadores < 2 && numJugadores > 5);
		//Queda por determinar el constructor de Tablero.
		Tablero tablero = new Tablero();
		
		List<Jugador> jugadores = new LinkedList<Jugador>();
		
		for (int i = 0; i < numJugadores; i++) {
			if (i == 0) {
				jugadores.add(new Jugador(null, i, true));
			} else {
				jugadores.add(new Jugador(null, i, false));
			}
		}
		
		while (tablero.getColonia().getMoral() != 0) {
		}
		
		//A los ganadores pantalla de victoria y a los perdedores pantalla de derrota. GUI.
		System.out.println("Has sobrevivido al duro invierno, figura. / Has muerto y das toda la peste, nene.");*/
	}
}
